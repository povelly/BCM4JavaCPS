# Lancement

La version de java doit être >= 8. De plus java doit être accessible dans un terminal seulement en tapant la commande "java".

Deploiement mono jvm, saisir dans un terminal : java -ea -cp ./bin:./lib/BCM4Java-10042020.jar deployment.CVM

